PORT=5000
NODE_ENV=production
CORS_ORIGIN=*
LOG_LEVEL=info

# ── Huawei Cloud RDS (PostgreSQL) ──
# Format: postgresql://<username>:<password>@<rds-internal-ip>:<port>/<database>
# Note: special characters in password must be URL-encoded (e.g. @ -> %40)
DATABASE_URL=postgresql://root:husa%40123@192.168.2.127:5432/pipeline_manager

# Set to "false" when app and RDS are in the same VPC
DB_SSL=false

# Connection pool size (adjust based on your RDS instance specs)
DB_POOL_MAX=10
